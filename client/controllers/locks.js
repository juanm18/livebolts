app.controller('lockController', function($scope, lockFactory){
  // lockFactory.getLock(function(data){
  //   $scope.lock = data;
  // })
  // $scope.updateStatus = function(lock, status){
  //   lockFactory.fupdateStatus(lock, status, function(data) {
  //     Lock.lock = status;
  //   })
  // }
  console.log(("lock controller"));
})
